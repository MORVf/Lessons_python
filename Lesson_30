'''
Напишите программу, которая считывает строку с числом n, которое задаёт количество чисел, которые нужно считать. 
Далее считывает n строк с числами xi, по одному числу в каждой строке. Итого будет n+1 строк.
При считывании числа xi программа должна на отдельной строке вывести значение f(xi). 
Функция вычисляется достаточно долго и зависит только от переданного аргумента x. Для того, чтобы уложиться в 
ограничение по времени, нужно избежать повторного вычисления значений.
'''

n = int(input()) #считываем число-количество строк

d = {} #инициализируем пустой словарь
s = [] #инициализируем пустой список

def f(x): #инициализация функции
    if x == 5:
        x = 11
    if x == 12:
        x = 41
    if x == 9:
        x = 47
    if x == 20:
        x = 61
    return (x)

for i in range(n): #считываем число с каждой строки
    x = int(input()) #вводится число
    s += [x] #записываем число в список
    if x not in d.keys(): #если такого числа-ключа ещё нет в словаре
        d[x] = f(x) #добавляем его в словарь в виде ключа вместе со значением = результату выполнения функции

for x in s: #для каждого числа списка = ключу словаря выводим значение по нему из словаря
    print(d[x], end='\n') #
