'''
Выведите таблицу размером n x n, заполненную числами от 1 до n^2 по спирали, выходящей из левого верхнего угла 
и закрученной по часовой стрелке, как показано в примере (здесь n = 5):

Sample Input:
5

Sample Output:
1 2 3 4 5
16 17 18 19 6
15 24 25 20 7
14 23 22 21 8
13 12 11 10 9
'''

n = int(input()) #вводим размер таблицы

field = [[0 for i in range(n)] for j in range(n)] #инициализируем матрицу и заполняем 0-ми

k = 1 #начальная цифра, которой будет заполняться матрица
t = n #сохраняем размер матрицы в другой переменной 
count = 1 #общий счетчик всех проходов, начальная нумерация проходов с 1 (с левого верхнего угла до конца вправо и в самый низ)
gggg = 0 #счетчик количества проходов именно по пути слева-направо-вниз
ffff = 0 #счетчик количества проходов именно по пути справа-налево-вверх
for n in range(n, 0, -1):#бежим внутрь матрицы от краёв до центра
    if count % 2 != 0: #если это проход по пути слева-направо-вниз
        j = 0 + gggg #выставляем j согласно счетчику проходов и размера матрицы
        for i in range(0 + gggg, n + gggg): #двигаемся слева направо
            while j < n + gggg:
                while k <= t * t:
                    field[i][j] = k #пишем в клетку цифру
                    k += 1 #увеличиваем цифру
                    break
                if j == n + gggg - 1: #если мы достигли края по вертикали, то мы сохраняем j, чтобы двигаться по нему вниз
                    break
                else:
                    j += 1 #идём дальше вправо
        gggg += 1 #увеличиваем количество проходов      
    else: #иначе это проход по пути справа-налево-вверх
        j = n + ffff - 1 #выставляем j согласно счетчику проходов и размера матрицы
        for i in range(n + ffff, 0 + ffff, -1): #двигаемся справа налево
            while j > ffff - 1:
                while k <= t * t:
                    field[i][j] = k #пишем в клетку цифру
                    k += 1 #увеличиваем цифру
                    break
                if j == ffff: #если мы достигли края по вертикали, то мы сохраняем j, чтобы двигаться по нему вверх
                    break
                else:
                    j -= 1 #идём дальше влево
        ffff += 1 #увеличиваем количество проходов   
    count += 1 #cчитываем проходы

#выводим список в виде таблицы
for k in range(t):
    for l in range(t):
        print(field[k][l], end=' ')
    print('')
